<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhangjunjie.dao.UserMapper">
    <resultMap id="UserMap" type="com.zhangjunjie.pojo.User">
        <!--column是数据库里面的字段，property是实体类中的属性-->
        <result column="id" property="id"></result>
        <result column="name" property="name"></result>
        <result column="pwd" property="password"></result>
    </resultMap>
<!--    <select id="getUserById" parameterType="int" resultMap="UserMap">-->
<!--&lt;!&ndash;        select id from mybatis.user where id=#{id}&ndash;&gt;-->
<!--        select * from mybatis.user where id=#{id}-->
<!--    </select>-->
    <select id="getUserByLimit" resultType="com.zhangjunjie.pojo.User">
        select * from mybatis.user LIMIT #{startindex},#{pagesize}
    </select>

</mapper>